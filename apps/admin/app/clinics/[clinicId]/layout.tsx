import { ClinicWorkspace } from '@/components/clinic-workspace'
export default function ClinicLayout({ children }: { children: React.ReactNode }) {
  return <ClinicWorkspace>{children}</ClinicWorkspace>
}
